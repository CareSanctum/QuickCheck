import { View, Text } from "react-native";

const Login = () => {
    return (
        <View className="flex-1 bg-chart5">
            <Text className="text-primary-foreground">Login2</Text>
        </View>
    )
}

export default Login;